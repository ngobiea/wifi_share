import 'package:flutter/material.dart';

class CreateCodePage extends StatelessWidget {
  final title = 'Create Code Page';
  const CreateCodePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Create Code Page',
      ),
    );
  }
}
